# AutoSearch-LLM
Built a temporal-aware LLM routing system that automatically detects time-sensitive queries and integrates live web search to reduce knowledge-cutoff hallucinations. Implemented query classification, tool-augmented generation, and FastAPI deployment. Achieved 80% routing accuracy on benchmark dataset.
